num b;
num a;

list[num] nums = { 1+1, nums[0], 3*2 } 

num v = nums[2]+nums[1]+nums[0]+1.1; 

proc createFiles(list[str] flList) {
    for(fileName in flLIst){
        file fl = fileName;
        if (fl.exists() = false){
            fl.create();  
        } 
        if {

        }
    } 
}

proc createFiles1(list[str] flList) {
    for(fileName in flLIst){
        file fl = fileName;
        if (fl.exists() = false){
            fl.create();  
        } 
    } 
}